// Replace the values below with your Firebase project config. This file is intentionally committed with placeholder values — do NOT commit secrets you don't want public.
// How to use: create a Firebase project, then replace these fields or create a new file named `firebase-config.js` with the same shape.

window.firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};

// Optionally set a simple admin UIDs array here for admin access to the admin dashboard.
// Example: window.easygradeAdmins = ['uid1','uid2'];
window.easygradeAdmins = [];
